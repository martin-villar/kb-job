# Interview assignament
- instalar dependencias: npm install
- iniciar mongo: $ mongod
- iniciar API: $ npm start

Endpoints:

- fetch all tasks:
/tasks

- fetch tasks by userId:
/tasks/userid

- fetch state of a task:
/task-state/taskId
